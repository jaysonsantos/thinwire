/target
**/*.rs.bk
.DS_Store
.env
.env.*
!.env.example
result
.direnv
.envrc

# TDLib on-disk artifacts (app-data only; never the repo)
td.binlog
**/td.binlog
*.binlog

# egui_kittest writes these beside the checked-in images when a snapshot fails
crates/thinwire/tests/snapshots/**/*.new.png
crates/thinwire/tests/snapshots/**/*.diff.png
crates/thinwire/tests/snapshots/**/*.old.png

# WhatsApp linked-device store (app-data only; never the repo)
device.sqlite
device.sqlite-*
